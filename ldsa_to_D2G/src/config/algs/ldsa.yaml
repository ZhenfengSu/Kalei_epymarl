# --- LDSA specific parameters ---

# use epsilon greedy action selector
action_selector: "epsilon_greedy"
epsilon_start: 1.0
epsilon_finish: 0.05
epsilon_anneal_time: 50000

runner: "episode"

buffer_size: 5000

# update the target network every {} episodes
target_update_interval: 200

# use the Q_Learner to train
agent_output_type: "q"
learner: "ldsa_learner"
double_q: True
mixer: "qmix"
mixing_embed_dim: 32
hypernet_layers: 2
hypernet_embed: 64

name: "ldsa"

agent: "ldsa"
mac: "ldsa_mac"
max_subtasks: 5
min_subtasks: 2
n_subtasks: 4
state_dims: None
agent_subtask_embed_dim: 64
sft_way: "gumbel_softmax" #softmax or gumbel_softmax
test_argmax: True
lambda_subtask_prob: 0.001
lambda_subtask_repr: 0.001
subtask_policy_use_hypernet: True
use_tanh: True
subtask_repr_layers: 2
random_sele: False # select a subtask randomly if True

cache_subtask_count: True  # 启用缓存
cache_steps: 10  # 每10步更新一次子任务数量
